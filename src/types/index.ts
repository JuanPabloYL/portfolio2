export type Activity = {
  name: string;
  icon: string;
  content: string;
};

export type Project = {
  name: string;
  date: string;
  description: string;
};

export type PortfolioProject = {
  name: string;
  url: string;
  description: string;
  imgURL: string;
};
